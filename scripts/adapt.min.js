define(function(){

	var ADAPT_CONFIG = {
	  // Where is your CSS?
	  path: '../css/adapt/',

	  // false = Only run once, when page first loads.
	  // true = Change on window resize and page tilt.
	  dynamic: true,

	  // First range entry is the minimum.
	  // Last range entry is the maximum.
	  // Separate ranges by "to" keyword.
	  range: [
	    '0px    to 760px  = mobile.css',
	    '760px  to 980px  = 720.css',
	    '980px  to 1280px = 960.css',
	    '1280px to 1600px = 1200.css',
	    '1600px to 1920px = 1560.css',
	    '1940px to 2540px = 1920.css',
	    '2540px           = 2520.css'
	  ]
	};

	(function(a, b, c, d) {
		function m(a, b) {
			l.href = e, f = e, h && h(a, b)
		}
		function n() {
			clearTimeout(g);
			for(var c, h, n, o, p, q, a = b.documentElement ? b.documentElement.clientWidth : 0, r = k, s = k - 1; r--;) if(e = "", c = j[r].split("="), h = c[0], q = c[1] ? c[1].replace(/\s/g, "") : d, p = h.match("to"), n = p ? parseInt(h.split("to")[0], 10) : parseInt(h, 10), o = p ? parseInt(h.split("to")[1], 10) : d, !o && r === s && a > n || a > n && o >= a) {
				q && (e = i + q);
				break
			}
			f ? f !== e && m(r, a) : (m(r, a), i && (b.head || b.getElementsByTagName("head")[0]).appendChild(l))
		}
		function o() {
			clearTimeout(g), g = setTimeout(n, 16)
		}
		if(c) {
			var e, f, g, h = "function" == typeof c.callback ? c.callback : d,
				i = c.path ? c.path : "",
				j = c.range,
				k = j.length,
				l = b.createElement("link");
			l.rel = "stylesheet", l.media = "screen", n(), c.dynamic && (a.addEventListener ? a.addEventListener("resize", o, !1) : a.attachEvent ? a.attachEvent("onresize", o) : a.onresize = o)
		}
	})(this, this.document, ADAPT_CONFIG);

})